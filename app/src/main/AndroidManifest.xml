<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pub2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/highbrowlogo"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.AppCompat">
        <activity android:name=".Activity.ParticipantsActivity"></activity>
        <activity android:name=".Activity.MatchDetailsActivity" />
        <activity android:name=".Activity.SplashScreenActivity" />
        <activity android:name=".Activity.ProfileCreationActivity" />
        <activity
            android:name=".Activity.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>